### Simple bind mount attach task. 

Attach the application (app.py) from the host into container using bind mount so that every time, there won't be a docker build required to run the container, always the updated code will run in the container
